# Workspaces

Generated runtime workspaces are intentionally excluded from this unified executable folder. Use `reports/` for new output.

This directory is a placeholder. It should not contain duplicated copies of the active package.

If a future experiment needs a workspace, keep generated files separate from `src/stock_rtx4060/` and record the reason in `reports/`.
