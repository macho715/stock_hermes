numpy>=1.26,<3.0
pandas>=2.2,<3.0
polars>=1.5
scipy>=1.13
scikit-learn>=1.1,<2.0
tabulate>=0.9
yfinance>=0.2.66
pykrx>=1.0.51
finance-datareader>=0.9.96
xgboost>=3.1.0
flask>=3.0
flask-cors>=4.0
# Phase 0 observability
loguru>=0.7
mlflow>=3.0
prometheus-client>=0.20
# Phase 1 PIT data lake (DuckDB+Parquet default; ArcticDB optional)
duckdb>=1.5.3
pyarrow>=17
httpx>=0.27
# Phase 2 RD-Agent Alpha Factory integration (all optional — graceful degradation when absent)
rdagent>=0.7,<1.0
pyqlib>=0.9.7

# Phase 4 portfolio optimization (all optional — pure-Python HRP fallback works without them)
skfolio>=0.4
cvxpy>=1.5
PyPortfolioOpt>=1.5
# Phase 3 ML upgrade (all optional — engine uses XGBoost path when missing)
optuna>=4.8  # 4.8.0 is current stable; v5 not yet on PyPI (2026-05-29)
shap>=0.46
lightgbm>=4.5
# Phase 5 backtest hardening (all optional — pure-NumPy fallbacks ship in-repo)
vectorbt>=0.26
arch>=7.0
statsmodels>=0.14
# Phase 6 LLM advisor layer (all optional — engine has a deterministic
# fallback when these are missing).
anthropic>=0.40
litellm>=1.55  # W2-B3: unified LLM gateway — Anthropic↔MiniMax fallback routing
langgraph>=0.2
feedparser
sec-edgar-downloader
pandas_datareader
jinja2
# Phase 7 orchestration & alert channels (all optional)
prefect>=3.0
slack-sdk
discord-webhook
# Phase 8 live broker layer (all optional — paper mode works without them)
alpaca-py>=0.30
ib_insync>=0.9.86
websockets>=13
vcrpy
